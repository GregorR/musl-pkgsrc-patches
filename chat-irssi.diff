# HG changeset patch
# Parent 0f016aecad19063a8b82d21100dadb2628cca336

diff -r 0f016aecad19 chat/irssi/Makefile
--- a/chat/irssi/Makefile	Mon Jul 08 19:19:37 2013 -0400
+++ b/chat/irssi/Makefile	Mon Jul 08 19:19:42 2013 -0400
@@ -18,11 +18,16 @@
 USE_LIBTOOL=		yes
 USE_TOOLS+=		pkg-config
 
+.include "../../mk/termcap.buildlink3.mk"
+
 CONFIGURE_ARGS+=	--with-terminfo \
 			--with-proxy \
-			--without-ncurses \
 			--sysconfdir=${PKG_SYSCONFDIR}
 
+.if "${TERMCAP_TYPE}" != "curses" || "${CURSES_TYPE}" != "ncurses"
+CONFIGURE_ARGS+=	--without-ncurses
+.endif
+
 EGDIR=		${PREFIX}/share/examples/irssi
 CONF_FILES=	${EGDIR}/irssi.conf ${PKG_SYSCONFDIR}/irssi.conf
 
