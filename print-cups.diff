# HG changeset patch
# Parent 1971655a81e224140d7632098aa4f3c916632572

diff -r 1971655a81e2 print/cups/Makefile
--- a/print/cups/Makefile	Mon Feb 18 12:30:34 2013 -0500
+++ b/print/cups/Makefile	Mon Feb 18 12:30:38 2013 -0500
@@ -71,8 +71,12 @@
 
 .if ${OPSYS} == "SunOS"
 CONFIGURE_ENV+=		LIBS_SSP=-lssp_nonshared
-LIBS.SunOS+=		-liconv
 .endif
+# [Gregor] Seems to be necessary on musl too
+LIBS+=		-liconv
+
+# [Gregor] Needs strl* functions, but excludes them
+CFLAGS+=	-D_BSD_SOURCE
 
 PLIST_VARS+=		notmac
 .if ${OPSYS} != "Darwin"
